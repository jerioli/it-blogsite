services:
  - type: web
    name: cybersafe-blog
    runtime: php
    buildCommand: "echo 'PHP app - no build required'"
    startCommand: "apache2-foreground"
    envVars:
      - key: PHP_VERSION
        value: 8.1