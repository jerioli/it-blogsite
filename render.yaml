services:
  - type: web
    name: cybersafe-blog
    runtime: php
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:$PORT index.php
    envVars:
      - key: PHP_VERSION
        value: 8.1